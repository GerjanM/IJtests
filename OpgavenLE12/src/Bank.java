/*public class Bank {
    //Rekening[] rekeningLijst = new Rekening[1000];
    public static final int MAXAANTALREKENINGEN = 1000;

    private int aantalRekeningen = 0;
    private Rekening[] rekeningLijst = new Rekening[MAXAANTALREKENINGEN];

    /*int rekening;
    public class Rekening{
        private int nummer;
        private String naam;

        public Rekening (String naam, int nummer){
            this.naam=naam;
            this.nummer=nummer;
            aantalRekeningen++
        }
    }

    private Rekening zoekRekening(int nummer) {
        for (int i = 0; i < rekeningLijst.length(); i++) {
            if (rekeningLijst[i].getNummer() == nummer) {
                return rekeningLijst[i];
            }
         }
        return null;
    }

    public void maakNieuweRekening(String naam, int nummer){
        if (rekeningLijst.zoekRekening(nummer)==null && aantalRekeningen<MAXAANTALREKENINGEN) {
            Rekening[] rekening = new Rekening(naam,nummer);
            rekeningLijst[aantalRekeningen]=rekening;
            aantalRekeningen++;
        }
      }

     public void stort(double bedrag,int nummer){
         Rekening rekening=zoekRekening(nummer);
         if (rekening!=null) {
             rekening.stort(bedrag);
         }
    }

    public void neemOp(double bedrag,Rekening rekeningOp, Rekening rekeningBij){

    }


    }
}
*/
